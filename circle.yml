machine:
  environment:
    IMAGE: johnswansonorg/tictag:$CIRCLE_SHA1
  services:
    - docker
dependencies:
  override:
    - echo "skipped"
compile:
  override:
    - ./bin/build
test:
  override:
    - ./bin/test
deployment:
  production:
    branch: master
    commands:
      - ./bin/deploy
